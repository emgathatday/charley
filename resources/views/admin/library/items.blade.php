@extends('layouts.master')

@section('title', 'Library Items')
@section('content_header')<div class="app-content-header"><div class="container-fluid"><div class="row align-items-center"><div class="col-sm-6"><h1 class="mb-0">Library Items</h1></div><div class="col-sm-6"><ol class="breadcrumb float-sm-end mb-0"><li class="breadcrumb-item"><a href="{{ route('admin.dashboard.library.index') }}">Library</a></li><li class="breadcrumb-item active">Items</li></ol></div></div></div></div>@endsection
@section('content')<div class="app-content"><div class="container-fluid"><div class="card card-outline card-primary mb-3"><div class="card-header"><h3 class="card-title mb-0">Filters</h3></div><div class="card-body"><form method="GET" action="{{ route('admin.dashboard.library.items') }}" class="row g-3 align-items-end"><div class="col-md-3"><label class="form-label" for="search">Search</label><input id="search" name="search" value="{{ $filters['search'] ?? '' }}" class="form-control" placeholder="Title"></div><div class="col-md-2"><label class="form-label" for="status">Status</label><select id="status" name="status" class="form-select"><option value="">All</option>@foreach ($statuses as $status)<option value="{{ $status }}" @selected(($filters['status'] ?? '') === $status)>{{ Str::headline($status) }}</option>@endforeach</select></div><div class="col-md-3"><label class="form-label" for="access_level">Access</label><select id="access_level" name="access_level" class="form-select"><option value="">All</option>@foreach ($accessLevels as $level)<option value="{{ $level }}" @selected(($filters['access_level'] ?? '') === $level)>{{ Str::headline($level) }}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label" for="content_type">Type</label><select id="content_type" name="content_type" class="form-select"><option value="">All</option>@foreach ($contentTypes as $type)<option value="{{ $type }}" @selected(($filters['content_type'] ?? '') === $type)>{{ Str::headline($type) }}</option>@endforeach</select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-primary" type="submit"><i class="bi bi-funnel me-1"></i>Filter</button><a class="btn btn-outline-secondary" href="{{ route('admin.dashboard.library.items') }}">Reset</a></div></form></div></div><div class="card card-outline card-primary"><div class="card-header d-flex justify-content-between"><h3 class="card-title mb-0">Items</h3><span class="badge text-bg-light">{{ $items->total() }} total</span></div><div class="card-body p-0"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Title</th><th>Category</th><th>Status</th><th>Access</th><th>File</th><th>Logs</th><th>Updated</th></tr></thead><tbody>@forelse ($items as $item)<tr><td><div class="fw-semibold">{{ $item->title }}</div><small class="text-body-secondary">{{ $item->author ?? 'Unknown author' }}</small></td><td>{{ $item->category?->title ?? '-' }}</td><td><span class="badge text-bg-{{ $item->status === 'published' ? 'success' : ($item->status === 'archived' ? 'secondary' : 'warning') }}">{{ Str::headline($item->status) }}</span></td><td>{{ Str::headline($item->access_level) }}</td><td>{{ $item->fileMedia?->original_name ?? '-' }}</td><td>{{ $item->access_logs_count }}</td><td>{{ optional($item->updated_at)->format('Y-m-d H:i') }}</td></tr>@empty<tr><td colspan="7" class="text-center text-body-secondary py-5">No library items found.</td></tr>@endforelse</tbody></table></div></div>@if ($items->hasPages())<div class="card-footer">{{ $items->withQueryString()->links() }}</div>@endif</div></div></div>@endsection
