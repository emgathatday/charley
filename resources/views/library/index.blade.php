@extends('layouts.master')

@section('title', 'Technical Library')

@section('content_header')
    <div class="app-content-header"><div class="container-fluid"><div class="row align-items-center"><div class="col-sm-6"><h1 class="mb-0">Technical Library</h1></div><div class="col-sm-6"><ol class="breadcrumb float-sm-end mb-0"><li class="breadcrumb-item"><a href="{{ route('feed.index') }}">Feed</a></li><li class="breadcrumb-item active">Library</li></ol></div></div></div></div>
@endsection

@section('content')
    <div class="app-content"><div class="container-fluid">
        @include('templates.components.alert-session')
        <div class="card card-outline card-primary mb-3"><div class="card-body"><form method="GET" action="{{ route('library.index') }}" class="row g-3 align-items-end"><div class="col-md-4"><label for="search" class="form-label">Search</label><input id="search" name="search" value="{{ $filters['search'] ?? '' }}" class="form-control" placeholder="Handbooks, videos, case studies"></div><div class="col-md-2"><label for="category_id" class="form-label">Category</label><select id="category_id" name="category_id" class="form-select"><option value="">All</option>@foreach ($categories as $category)<option value="{{ $category->id }}" @selected((string) ($filters['category_id'] ?? '') === (string) $category->id)>{{ $category->title }}</option>@endforeach</select></div><div class="col-md-2"><label for="content_type" class="form-label">Type</label><select id="content_type" name="content_type" class="form-select"><option value="">All</option>@foreach ($contentTypes as $type)<option value="{{ $type }}" @selected(($filters['content_type'] ?? '') === $type)>{{ Str::headline($type) }}</option>@endforeach</select></div><div class="col-md-2"><label for="access_level" class="form-label">Access</label><select id="access_level" name="access_level" class="form-select"><option value="">All</option>@foreach ($accessLevels as $level)<option value="{{ $level }}" @selected(($filters['access_level'] ?? '') === $level)>{{ Str::headline($level) }}</option>@endforeach</select></div><div class="col-md-2 d-flex gap-2"><button class="btn btn-primary" type="submit"><i class="bi bi-search me-1"></i>Search</button><a href="{{ route('library.index') }}" class="btn btn-outline-secondary">Reset</a></div></form></div></div>
        <div class="row g-3 mb-3">@foreach ($featuredItems as $featured)<div class="col-md-6 col-xl-3"><div class="card h-100 card-outline card-light"><div class="card-body"><span class="badge text-bg-info mb-2">{{ Str::headline($featured->content_type) }}</span><h2 class="h6"><a href="{{ route('library.items.show', $featured) }}" class="link-dark text-decoration-none">{{ $featured->title }}</a></h2><p class="small text-body-secondary mb-0">{{ Str::limit($featured->summary, 95) }}</p></div></div></div>@endforeach</div>
        <div class="row g-3">@forelse ($items as $item)<div class="col-md-6 col-xl-4"><div class="card h-100 card-outline card-primary"><div class="card-body d-flex flex-column"><div class="d-flex justify-content-between gap-2 mb-2"><span class="badge text-bg-light">{{ $item->category?->title ?? 'Library' }}</span><span class="badge text-bg-{{ $item->access_level === 'public' ? 'success' : 'warning' }}">{{ Str::headline($item->access_level) }}</span></div><h2 class="h5 mb-2"><a href="{{ route('library.items.show', $item) }}" class="link-dark text-decoration-none">{{ $item->title }}</a></h2><p class="text-body-secondary small mb-3">{{ Str::limit($item->summary, 140) }}</p><div class="mt-auto d-flex justify-content-between align-items-center"><span class="small text-body-secondary">{{ $item->author ?? 'Charley Library' }}</span><a href="{{ route('library.items.show', $item) }}" class="btn btn-sm btn-outline-primary"><i class="bi bi-box-arrow-up-right me-1"></i>Open</a></div></div></div></div>@empty<div class="col-12"><div class="card"><div class="card-body text-center text-body-secondary py-5">No library items found.</div></div></div>@endforelse</div>
        @if ($items->hasPages())<div class="mt-3">{{ $items->withQueryString()->links() }}</div>@endif
    </div></div>
@endsection
